# /etc/systemd/system/borg-backup.mount

[Unit]
Description=BorgBackup Mount

[Mount]
What=/dev/sdb1
Where=/var/backup
Type=xfs
Options=defaults
#DirectoryMode=0755

[Install]
WantedBy=multi-user.target
