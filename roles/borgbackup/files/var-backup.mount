# /etc/systemd/system/var-backup.mount

[Unit]
Description=BorgBackup Mount

[Mount]
What=UUID={{ blkid_sdb1.stdout }}
Where=/var/backup
Type=xfs
Options=defaults, uid={{ uid_borg.stdout }}, gid={{ gid_borg.stdout }}
#DirectoryMode=0755

[Install]
WantedBy=multi-user.target
